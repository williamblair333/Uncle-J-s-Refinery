# tinycount

Count the words in a text file.

## Install

```sh
apt-get update && apt-get install -y python3
```

## Use

```sh
python3 tinycount.py sample.txt
```

Prints the count and writes it to `/tmp/tinycount.out`.
