{
  "_comment": "Generated from windsurf-mcp.json.tmpl by install.sh. Drop the rendered mcp-clients/windsurf-mcp.json into ~/.codeium/windsurf/mcp_config.json (Windsurf uses the same `mcpServers` schema).",
  "_comment_placeholders": "{{STACK_VENV_BIN}} -> absolute path to the venv binary dir. ",

  "mcpServers": {
    "jcodemunch": {
      "command": "{{STACK_VENV_BIN}}/jcodemunch-mcp{{EXE}}"
    },
    "jdatamunch": {
      "command": "{{STACK_VENV_BIN}}/jdatamunch-mcp{{EXE}}"
    },
    "jdocmunch": {
      "command": "{{STACK_VENV_BIN}}/jdocmunch-mcp{{EXE}}"
    },
    "mempalace": {
      "command": "{{STACK_VENV_BIN}}/python{{EXE}}",
      "args": ["-m", "mempalace.mcp_server"]
    },
    "serena": {
      "command": "uvx",
      "args": [
        "--from", "git+https://github.com/oraios/serena",
        "serena", "start-mcp-server",
        "--context", "ide-assistant"
      ]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "duckdb": {
      "command": "uvx",
      "args": [
        "mcp-server-motherduck",
        "--db-path", ":memory:",
        "--read-write",
        "--allow-switch-databases"
      ]
    }
  }
}
